function showDeleteConfirmation(roleId) {
    document.getElementById("confirmDelete-" + roleId).style.display = "block";
}

function hideDeleteConfirmation(roleId) {
    document.getElementById("confirmDelete-" + roleId).style.display = "none";
}
